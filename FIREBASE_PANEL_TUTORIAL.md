# 📱 آموزش تصویری پنل Firebase - قدم به قدم

## 🎯 هدف: فعال کردن Realtime Database برای دیدن آمار کاربرها

---

## قدم 1️⃣: ورود به Firebase Console

1. برو به این لینک: **https://console.firebase.google.com**
2. با اکانت Google خودت لاگین کن
3. پروژه **ReSynth** رو پیدا کن و روش کلیک کن

**یا مستقیم برو اینجا:**
👉 https://console.firebase.google.com/project/resynth-b44bb

---

## قدم 2️⃣: رفتن به Realtime Database

در صفحه پروژه:

1. از **منوی سمت چپ**، پیدا کن: **"Build"**
2. زیرش کلیک کن روی: **"Realtime Database"**

```
📁 Build
   ├─ Authentication
   ├─ Firestore Database
   ├─ Realtime Database  👈 اینجا کلیک کن!
   ├─ Storage
   └─ ...
```

**لینک مستقیم:**
👉 https://console.firebase.google.com/project/resynth-b44bb/database

---

## قدم 3️⃣: ساخت Database

اگه Realtime Database فعال نیست، یه صفحه میاد که میگه:
**"Get started by adding Realtime Database to your app"**

### کارهایی که باید بکنی:

**۱. کلیک روی دکمه بزرگ آبی:**
```
┌─────────────────────────────────┐
│   Create Database              │  👈 اینجا کلیک کن
└─────────────────────────────────┘
```

**۲. یه پنجره میاد که میپرسه Location کجا باشه:**

```
┌──────────────────────────────────────────┐
│  Set up database                         │
│                                          │
│  Choose a location for your database    │
│                                          │
│  ▼ United States (us-central1)          │  👈 اینو انتخاب کن
│                                          │      (یا europe-west1)
│  [ Next ]                                │
└──────────────────────────────────────────┘
```

انتخاب کن یکی از اینا:
- ✅ **United States (us-central1)** - سریع‌تر
- ✅ **Europe (europe-west1)** - نزدیک‌تر به ایران

**۳. بعد کلیک کن روی: "Next"**

**۴. Security Rules رو انتخاب کن:**

```
┌──────────────────────────────────────────────────────┐
│  Security rules for Realtime Database               │
│                                                      │
│  ○ Start in locked mode                             │
│     (دسترسی به هیچکس داده نمیشه - نزن!)              │
│                                                      │
│  ● Start in test mode  👈 این رو انتخاب کن          │
│     (برای تست - همه دسترسی دارن - 30 روز)          │
│                                                      │
│  [ Enable ]                                          │
└──────────────────────────────────────────────────────┘
```

**⚠️ مهم:** حتماً **"Start in test mode"** رو انتخاب کن!

**۵. کلیک کن روی: "Enable"**

صبر کن چند ثانیه... ✨

---

## قدم 4️⃣: تنظیم Security Rules (مهم!)

بعد از اینکه Database ساخته شد، باید قوانین امنیتی رو درست تنظیم کنی:

### ۱. برو به تب "Rules"

```
┌─────────────────────────────────────────┐
│  Data  │  Rules  │  Backups  │  Usage  │
│        │   👆                            │
│        │  اینجا                          │
└─────────────────────────────────────────┘
```

### ۲. کد قدیمی رو پاک کن و این رو جاش بذار:

```json
{
  "rules": {
    "users": {
      "$userId": {
        ".read": true,
        ".write": true
      }
    },
    "stats": {
      ".read": true,
      ".write": true
    }
  }
}
```

### ۳. کلیک کن روی: "Publish"

```
┌──────────────────────────────────────┐
│  Rules                               │
│  ┌────────────────────────────────┐  │
│  │ {                              │  │
│  │   "rules": {                   │  │
│  │     "users": { ... }           │  │
│  │   }                            │  │
│  │ }                              │  │
│  └────────────────────────────────┘  │
│                                      │
│  [ Publish ]  👈 اینجا کلیک کن      │
└──────────────────────────────────────┘
```

یه پیغام تایید میاد، بزن **"Publish"** دوباره.

---

## قدم 5️⃣: چک کردن که درست کار کرد

### برو به تب "Data"

```
┌─────────────────────────────────────────┐
│  Data  │  Rules  │  Backups  │  Usage  │
│   👆                                     │
│  اینجا                                   │
└─────────────────────────────────────────┘
```

باید یه چیزی شبیه این ببینی:

```
┌──────────────────────────────────────────────┐
│  resynth-b44bb-default-rtdb                 │
│                                              │
│  🔍 Filter                                   │
│  ┌────────────────────────────────────────┐ │
│  │ ∅ null                                 │ │
│  │                                        │ │
│  │ (هنوز دیتایی نیست - نرمال است!)       │ │
│  └────────────────────────────────────────┘ │
└──────────────────────────────────────────────┘
```

این یعنی Database آماده‌س! ✅

---

## قدم 6️⃣: کپی کردن Database URL (اختیاری)

اگه بخوای URL دیتابیست رو ببینی:

۱. برو بالای صفحه، یه لینک میبینی مثل:
```
https://resynth-b44bb-default-rtdb.firebaseio.com/
```

۲. این همون URL دیتابیسته که توی کد Dashboard استفاده شده!

---

## ✅ تموم شد! حالا چی؟

### گام بعدی: ساخت و نصب اپ

باید اپ رو بسازی و روی گوشی نصب کنی تا داده‌ها بیان:

```bash
# توی پوشه پروژه
cd C:\Users\R3ZA\ReSynthApp-temp

# پاک کردن فایل‌های قدیمی
flutter clean

# نصب پکیج‌ها
flutter pub get

# ساخت APK
flutter build apk --release
```

فایل APK اینجا میسازه:
```
C:\Users\R3ZA\ReSynthApp-temp\build\app\outputs\flutter-apk\app-release.apk
```

### بعد از نصب اپ:

۱. برگرد به Firebase Console → Realtime Database → Data
۲. باید ببینی که `users` و `stats` ساخته شدن!
۳. یه نفر (خودت) توی `users` ثبت میشه

مثلا:
```
resynth-b44bb-default-rtdb
├── users
│   └── android_abc123def
│       ├── first_seen: "2025-12-04..."
│       ├── last_seen: "2025-12-04..."
│       ├── platform: "android"
│       └── app_version: "1.0.0"
└── stats
    └── daily
        └── 2025-12-04
            ├── active_users: 1
            └── app_opens: 1
```

---

## 📊 دیدن آمار زنده

### روش 1: Firebase Console
برو به: https://console.firebase.google.com/project/resynth-b44bb/database/resynth-b44bb-default-rtdb/data

### روش 2: Dashboard سفارشی (پیشنهادی!)
باز کن فایل رو با مرورگر:
```
C:\Users\R3ZA\ReSynthApp-temp\firebase\dashboard.html
```

یه Dashboard زیبا میبینی با:
- 📊 Total Users
- ⚡ Active Today
- 🔌 Connections Today
- 👥 Currently Connected

---

## 🔥 نکات امنیتی مهم!

### ⚠️ بعد از تست:

Security Rules فعلی برای **تست** هست! بعداً باید سخت‌ترش کنی:

```json
{
  "rules": {
    "users": {
      "$userId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },
    "stats": {
      ".read": true,
      ".write": "auth != null"
    }
  }
}
```

این فقط به کاربرهای Authentication شده اجازه نوشتن میده.

---

## 🆘 مشکلات رایج:

### مشکل 1: دکمه "Create Database" نمیاد
**راه‌حل:** احتمالاً قبلاً ساختی! برو مستقیم به "Data" تب.

### مشکل 2: خطا هنگام Publish Rules
**راه‌حل:**
- مطمئن شو JSON درست نوشته شده (کاما و براکت‌ها)
- Refresh کن صفحه رو و دوباره امتحان کن

### مشکل 3: آمار نمیاد بعد از نصب اپ
**راه‌حل:**
- مطمئن شو Rules رو درست تنظیم کردی
- مطمئن شو اپ رو باز کردی (نه فقط نصب!)
- صبر کن 10 ثانیه و Refresh کن صفحه رو

### مشکل 4: "Permission Denied"
**راه‌حل:** Rules رو چک کن، باید `"test mode"` باشه.

---

## 📸 چک‌لیست نهایی:

قبل از بستن Firebase Console، این‌ها رو چک کن:

- [ ] ✅ Realtime Database ساخته شد؟
- [ ] ✅ Location انتخاب شد؟ (US یا Europe)
- [ ] ✅ "Start in test mode" انتخاب شد؟
- [ ] ✅ Security Rules تنظیم شد؟
- [ ] ✅ Rules منتشر (Publish) شد؟
- [ ] ✅ Data تب خالیه؟ (نرماله!)
- [ ] ✅ Database URL کپی شد؟ (اختیاری)

اگه همه ✅ بود، آماده‌ای! 🎉

---

## 🔗 لینک‌های مفید:

- **Firebase Console:** https://console.firebase.google.com/project/resynth-b44bb
- **Realtime Database:** https://console.firebase.google.com/project/resynth-b44bb/database
- **Analytics:** https://console.firebase.google.com/project/resynth-b44bb/analytics
- **GitHub Repo:** https://github.com/syshe2840-spec/ReSynthApp

---

## 💡 نکته آخر:

بعد از نصب اپ، می‌تونی آمار زنده رو اینجا ببینی:
1. Firebase Console (Data تب)
2. Dashboard HTML (زیباتره!)
3. Analytics Dashboard (آمار عمومی‌تر)

**موفق باشی!** 🚀

---

Made with ❤️ by ReSynth Team
